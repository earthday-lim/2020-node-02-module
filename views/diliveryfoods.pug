<!DOCTYPE html>
html(lang="ko")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		title 배달음식 리스트
		link(rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css")
	body
		.container
			table.table
				thead.thead-dark
					tr
						th 번호
						th 이름
						th 창업자
				tbody
					each item in foods
						tr
							td= item.id
							td= item.title
							td= item.content
							//- pug에선 들여쓰기 중요, 뒤에 공백있으면 에러 -> vscode > view > render control charater check